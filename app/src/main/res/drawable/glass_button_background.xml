<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Fundo translúcido para efeito glassmorphism -->
    <solid android:color="@color/glass_white_translucent" />

    <!-- Cantos arredondados -->
    <corners android:radius="12dp" />

    <!-- Borda com gradiente -->
    <stroke
        android:width="1dp"
        android:color="@color/primary_purple" />

    <!-- Gradiente para efeito glass -->
    <gradient
        android:angle="45"
        android:startColor="@color/glass_white_translucent"
        android:endColor="@color/glass_white_translucent"
        android:type="linear" />

</shape>
